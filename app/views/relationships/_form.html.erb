<%= form_for(@relationship) do |f| %>
  <% if @relationship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@relationship.errors.count, "error") %> prohibited this relationship from being saved:</h2>

      <ul>
      <% @relationship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person1_index %><br />
    <%= f.text_field :person1_index %>
  </div>
  <div class="field">
    <%= f.label :person2_index %><br />
    <%= f.text_field :person2_index %>
  </div>
  <div class="field">
    <%= f.label :original_certainty %><br />
    <%= f.text_field :original_certainty %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
