<h2>Inactive Group Categories</h2>
<% if ! @inactive_group_cats.empty? %>
  <div class = "well">
    <table class="table table-condensed">
      <thead>
        <tr>
          <th>SDFB Group Category ID</th>
          <th>Group Category Name</th>
          <th>Description</th>
          <th><%=t '.actions', :default => t("helpers.actions") %></th>
        </tr>
      </thead>
      <tbody>
        <% @inactive_group_cats.each do |group_category| %>
          <tr>
            <td><%= group_category.id %></td>
            <td><%= group_category.name %></td>
            <td><%= group_category.description %></td>
            <td>
              <% if can? :show, group_category %>
                <%= link_to 'Show', group_category, :class => 'btn btn-mini' %>
              <% end %>
              <% if can? :update, group_category %>
                <%= link_to t('.edit', :default => t("helpers.links.edit")),
                                edit_group_category_path(group_category), :class => 'btn btn-mini' %>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <%= will_paginate @inactive_group_cats, :param_name => 'inactive_group_cats_page', :previous_label => "Previous&nbsp;", :next_label => "&nbsp;Next" %>
  </div>
<% else %>
  <div class = "well">
    No inactive group categories exist.
  </div>
<% end %>