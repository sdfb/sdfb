// One thing to note is that for the tablesorter.js library I left out a file named JQuery-latest because it broke our autocomplete code for the search functions. Any bugs with table sorting may ensue from this.
$(document).ready(function() 
    { 
        $("#myTable").tablesorter(); 
    } 
); 
    