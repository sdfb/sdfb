<%- model_class = UserGroupContrib -%>

<div class="row">
  <div class="span12">
    <h1>Group Note</h1>
  </div>
</div>
<br>
<div class="row">
  <div class="span5">
    <div class="well">
      <h2>Information
        <% if can? :update, @user_group_contrib %> 
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                edit_user_group_contrib_path(@user_group_contrib), :class => 'btn btn-mini' %>
        <% end %>
      </h2>
      <br>
      <dl class="dl-horizontal">
        <dt><strong>SDFB Group Annot. ID:</strong></dt>
        <dd><%= @user_group_contrib.id %></dd>
        <dt><strong>Group:</strong></dt>
        <dd><%= link_to Group.find(@user_group_contrib.group_id).name,  group_path(@user_group_contrib.group_id) %></dd>
        <dt><strong>Note:</strong></dt>
        <dd><%= @user_group_contrib.annotation %></dd>
        <% if ! @user_group_contrib.bibliography.blank? %>
          <dt><strong>Citation:</strong></dt>
          <dd><%= @user_group_contrib.bibliography %></dd>
        <% end %>
        <dt><strong>Created:</strong></dt>
        <dd><%= @user_group_contrib.created_at.strftime("%m/%d/%Y") %> by <%= link_to User.find(@user_group_contrib.created_by).username,  user_path(@user_group_contrib.created_by) %></dd>
        <% @last_edit = @user_group_contrib.last_edit %>
        <% if ! @last_edit.blank? %>
          <dt><strong>Last Edit:</strong></dt>
          <dd><%= @last_edit[1].strftime("%m/%d/%Y")%> by <%= link_to User.find(@last_edit[0]).username,  user_path(@last_edit[0]) %></dd>
        <% end %>
        <% if (! current_user.blank?) && ((current_user.user_type == "Admin") || (current_user.user_type == "Curator")) %>
          <dt><strong>Is Approved?:</strong></dt>
          <dd><%= @user_group_contrib.is_approved %></dd>
          <% if ! @user_group_contrib.approved_by.blank? %>
            <dt><strong>Approved By:</strong></dt>
            <dd><%= link_to User.find(@user_group_contrib.approved_by).username,  user_path(@user_group_contrib.approved_by) %></dd>
          <% end %>
          <% if ! @user_group_contrib.approved_by.blank? %>
            <dt><strong>Approved On:</strong></dt>
            <dd><%= @user_group_contrib.approved_on %></dd>
          <% end %>
          <dt><strong>Is Active:</strong></dt>
          <dd><%= @user_group_contrib.is_active %></dd>
          <dt><strong>Is Rejected:</strong></dt>
          <dd><%= @user_group_contrib.is_rejected %></dd>
        <% end %>
      </dl>
    </div>
  </div>
</div>